<%= form_for @user,:html=>{:class=>".form-horizontal"} do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label(:account,:class=>"control-label span2") %>
    <div class="controls">
        <%= f.text_field :account,:placeholder=>"Account" %>
    </div>

  </div>
  <div class="control-group">
    <%= f.label :email,:class=>"control-label span2" %>
    <%= f.text_field :email,:placeholder=>"Email" %>
  </div>
  <div class="control-group">
    <%= f.label :dynamic_desc,:class=>"control-label span2" %>
    <%= f.text_field :dynamic_desc,:placeholder=>"Dynamic desc" %>
  </div>
  <div class="control-group">
    <%= f.submit %>
  </div>
<% end %>
